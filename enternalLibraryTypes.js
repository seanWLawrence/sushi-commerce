declare module 'react-fontawesome' {
  declare type FA = {
    name: string,
    ariaLabel?: string,
    border?: boolean,
    className?: string,
    cssModule?: object,
    fixedWidth?: boolean,
    flip?: boolean,
    inverse?: boolean,
    pulse?: string,
    rotate?: number,
    size?: string,
    stack?: string
  }
}

declare module 'graphql' {
  decalre type GraphQL = string
}
