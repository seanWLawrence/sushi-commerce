describe('TODO: Features', () => {
  describe('TODO: Landing features', () => {
    test('displays list of features with titles, icons and text', () => {});
  });

  describe('TODO: Product features', () => {
    test('splits the text input by periods and displays them as <li> elements', () => {});

    test('displays one <li> element if no periods are in the text', () => {});
  });
});
