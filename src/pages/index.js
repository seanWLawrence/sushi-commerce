import React from 'react'
import bulmaClassnames from '../utils'

const IndexPage = () => (
  <div>
    <h1 className={bulmaClassnames({textColor: 'primary'})}>Hi people</h1>
    <p>Welcome to your new Gatsby site.</p>
  </div>
);

export default IndexPage
