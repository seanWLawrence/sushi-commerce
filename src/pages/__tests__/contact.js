describe('TODO: Contact page', () => {
  test('Contact form is rendered with title, email, message and send message button', () => {});
  test('Page is redirected on successful submission', () => {});
});
