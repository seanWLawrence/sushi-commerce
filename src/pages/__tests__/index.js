describe('TODO: Landing page', () => {
  test('displays banner with heading, subheading and button', () => {});
  test('displays about with heading and text', () => {});
  test('displays icons with titles, icon and text', () => {});
  test('displays testimonials with name and testimonial', () => {});
  test('displays banner with eheading, subheading and button', () => {});
});
